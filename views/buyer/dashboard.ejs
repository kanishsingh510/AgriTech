<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Purchases</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <script> tailwind.config = { theme: { extend: { colors: { primary: '#3CB043' } } } } </script>
  <style> body{font-family:Inter,ui-sans-serif,system-ui;} </style>
</head>
<body class="min-h-screen bg-gray-50 text-gray-800">
  <header class="bg-white/90 backdrop-blur shadow-sm sticky top-0 z-20">
    <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
      <a href="/" class="flex items-center gap-2">
        <span class="inline-flex h-9 w-9 items-center justify-center rounded-lg bg-primary/10 text-primary font-bold">Ag</span>
        <span class="font-semibold text-lg">AgriTech</span>
      </a>
      <nav class="hidden md:flex items-center gap-6 text-sm">
        <a href="/buyer/marketplace" class="hover:text-primary">Marketplace</a>
        <a href="/buyer/dashboard" class="text-primary font-medium">My Purchases</a>
      </nav>
      <form action="/logout" method="POST"><button class="px-3 py-2 rounded-lg bg-primary text-white text-sm hover:opacity-90">Logout</button></form>
    </div>
  </header>

  <main class="max-w-7xl mx-auto px-6 py-8">
    <h1 class="text-2xl md:text-3xl font-semibold mb-6">My Purchases</h1>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% transactions.forEach(t => { const p=t.productId||{}; %>
      <article class="bg-white rounded-xl shadow-sm hover:shadow-md transition-shadow overflow-hidden">
        <div class="relative aspect-[4/3] bg-gray-100">
          <img src="<%= p.image || p.imagePath || 'https://images.unsplash.com/photo-1501004318641-b39e6451bec6?q=80&w=1200&auto=format&fit=crop' %>" class="w-full h-full object-cover" alt="<%= p.name||'Product' %>" />
          <span class="absolute top-3 left-3 inline-flex text-xs px-2 py-1 rounded-full bg-primary/90 text-white"><%= t.paymentStatus %></span>
        </div>
        <div class="p-4 space-y-1">
          <div class="font-semibold"><%= p.name || 'Product' %></div>
          <div class="text-primary font-bold">₹ <%= Number(t.amount).toFixed(2) %></div>
          <div class="text-xs text-gray-500"><%= new Date(t.createdAt).toLocaleString() %></div>
        </div>
      </article>
      <% }) %>
    </div>
  </main>
</body>
</html>







